package class11;

public class Notes {
	
	
	/*
	 * Day-11

	Methods
	Block or group of statements which will perform a certain task.
	We have to call the method through an object.
	1) No params, No return value
	2) No params, Return value
	3) Takes params, No return value
	4) Takes params, Returns value

	String s = new String(welcome");
	int len=s.lenght() --- no parameters, returns value
	
	String sub=s.substring(2,5) ---Takes params, Returns value

	How many ways we can store data into variables:
	1.By using object reference variable
	2.Method
	3.Constructor

	Methods
	1)Method may or may not return a value.
	2)Method may or may not carry arguments.
	3)If a method is not returning any value, then specify void.
	4)Method can take parameters/arguments.
	5)We have to invoke/call methods explicitly through an object.
	
	Constructor:
	
	1)Constructor never returns a value (not even void).
	2)Constructor name should be the same as the class name.
	3)We don’t specify void.
	4)Constructor can take parameters/arguments.
	5)Constructor is automatically invoked at the time of object creation
	6) used for initializing the values of the variables.
	 
	 Constructor:
	 1)Default constructor
	 2) parameterized constructor
	 
	 *
	 *
	 */

}
