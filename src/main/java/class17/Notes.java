package class17;

public class Notes {
	
	/*
	 * Wrapper classes:
	   int — Integer
		float — Float
		double — Double
		char — Character
		boolean — Boolean
		
		String price1 = “150.50”; 
		String price2 = “120.50”;
		price1 + price2

		Scenario 1: String → int,double, boolean 
		Scenario 2: int, double, boolean, char → String
	 
		String to int --> Integer.parseInt(String value)
		String to double--> Double.parseDouble(String value)
		String to boolean-->Boolean.parseBoolean(String value)
		String to char-->Not possible directly
		
		Any data type --->String
		String.valueOf()--> Convert all primitive data type into String
		 
		ArrayList arr = new ArrayList(); // any type of data
		ArrayList<Integer> arr = new ArrayList<Integer>(); // Valid ( to restrict data types)
		ArrayList<int> arr = new ArrayList<int>(); // Not valid
	 
		 packages and access modifiers
		 -----------------------------
		 Built-in-package --> ex java.util
		 user defined package--> class01 to class07
		 
	 	access modifiers
	 	------------------
	 	1)public - variables and methods can be accessible within the project
	 	2)protected - accesible within the package for protected we need to extends Test1
	 	3)default - only within the package
	 	4)private - access only within the class


		public: Directly access all variables & methods everywhere.
		protected: Accessible outside of the package through inheritance.
		default: Only within the package (no modifier).
		private: Access only within the class.
	 */

}
